[package]
name = "oracle"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
miden = { version = "0.14" }

[build-dependencies]
miden-sdk-build-script-support = { version = "0.14" }
